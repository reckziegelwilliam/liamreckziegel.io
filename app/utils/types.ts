export interface TimelineEvent {
  year: number;
  title: string;
  description: string;
}

export interface PathDimensions {
  width: number;
  height: number;
  scale: number;
  opacity: number;
}

export interface TunnelCircle {
  size: number;
  depth: number;
  opacity: number;
}